<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cty.nopersonfinally.mapper.FlyerComplaintMapper">

    <select id="getByTargetId" resultType="com.cty.nopersonfinally.pojo.entity.FlyerComplaint">
        SELECT * FROM flyer_complaint
        WHERE target_id = #{targetId}
        ORDER BY created_at DESC
    </select>

    <select id="getByOrderAndReporter" resultType="com.cty.nopersonfinally.pojo.entity.FlyerComplaint">
        SELECT * FROM flyer_complaint
        WHERE order_id = #{orderId} AND reporter_id = #{reporterId}
    </select>

    <select id="countPendingComplaints" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM flyer_complaint
        WHERE status = 0
    </select>

</mapper>
