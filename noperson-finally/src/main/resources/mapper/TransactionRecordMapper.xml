<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cty.nopersonfinally.mapper.TransactionRecordMapper">

    <!-- 查询用户的交易记录 -->
    <select id="selectUserTransactions" resultType="com.cty.nopersonfinally.pojo.entity.TransactionRecord">
        SELECT * FROM transaction_record
        WHERE user_id = #{userId}
        <if test="startTime != null">
            AND create_time >= #{startTime}
        </if>
        <if test="endTime != null">
            AND create_time &lt;= #{endTime}
        </if>
        <if test="transactionType != null and transactionType != ''">
            AND transaction_type = #{transactionType}
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 统计用户指定时间段内的收入总额 -->
    <select id="selectUserIncomeSum" resultType="java.lang.Double">
        SELECT SUM(amount) FROM transaction_record
        WHERE user_id = #{userId}
        AND transaction_type = 'INCOME'
        AND status = 'success'
        <if test="startTime != null">
            AND create_time >= #{startTime}
        </if>
        <if test="endTime != null">
            AND create_time &lt;= #{endTime}
        </if>
    </select>

    <!-- 统计用户指定时间段内的支出总额 -->
    <select id="selectUserExpenseSum" resultType="java.lang.Double">
        SELECT SUM(ABS(amount)) FROM transaction_record
        WHERE user_id = #{userId}
        AND transaction_type = 'EXPENSE'
        AND status = 'success'
        <if test="startTime != null">
            AND create_time >= #{startTime}
        </if>
        <if test="endTime != null">
            AND create_time &lt;= #{endTime}
        </if>
    </select>

</mapper>